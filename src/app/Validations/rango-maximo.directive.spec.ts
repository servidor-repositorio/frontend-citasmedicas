import { RangoMaximoDirective } from './rango-maximo.directive';

describe('RangoMaximoDirective', () => {
  it('should create an instance', () => {
    const directive = new RangoMaximoDirective();
    expect(directive).toBeTruthy();
  });
});
